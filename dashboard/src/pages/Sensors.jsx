export default function Sensors() {
    return (
        <section className="page">
            <h1>Sensors</h1>
            <p>View all stored sensor readings from SQLite.</p>
        </section>
    );
}