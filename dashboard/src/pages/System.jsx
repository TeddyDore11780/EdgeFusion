export default function System() {
    return (
        <section className="page">
            <h1>System</h1>
            <p>System health for Gateway, MQTT, OpenFaaS and SQLite.</p>
        </section>
    );
}