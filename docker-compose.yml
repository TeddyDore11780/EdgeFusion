services:

  mosquitto:
    build:
      context: ./mqtt
    container_name: edgefusion-mqtt

    ports:
      - "1883:1883"

    volumes:
      - mqtt-data:/mosquitto/data
      - mqtt-log:/mosquitto/log

    restart: unless-stopped

volumes:
  mqtt-data:
  mqtt-log:
